# Auth tokens & secrets
tokens.json
mcp_config.json

# Role-ID cache (contains real hub role UUIDs — resolve names locally, don't commit)
role_id_cache.json

# Virtual environment
.venv/

# Audit output files
audit_*.csv

# Claude Code project config (personal hooks/memory — not shared)
.claude/

# Local work tracking
todo.md
test_plan_*.md

# Archived / scratch files
archive/

# Python cache
__pycache__/
*.pyc
.pytest_cache/
